[project]
name = "discord-no-disturb-bot"
version = "2.0.6"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "basicdiscordbot>=2.0.2",
    "discord-py>=2.7.1",
    "dotenv>=0.9.9",
    "python-dotenv>=1.2.3",
]

[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "uv"
tag_format = "v$version"
update_changelog_on_bump = true
